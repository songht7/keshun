<!--pages/carrier/driver/index.wxml-->
<view class="container">
	<panel-list list="{{list}}" bind:onTap="onTap" bind:search="search" storageSearch="{{false}}" field="{{field}}" holder="搜索司机姓名"></panel-list>
	<fixed-btns addBtn="{{addBtn}}"></fixed-btns>
</view>